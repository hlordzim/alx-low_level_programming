#include "main.h"
#include <stddef.h>

/**
 * leet - Encodes a string into "1337".
 * @str: The input string to encode.
 *
 * Return: A pointer to the modified string.
 */
char *leet(char *str)
{
	char *leet_replace = "aAeEoOtTlL";
	char *leet_replace_with = "443307711";

	if (str == NULL)
		return (NULL);
}
